# Canonical production HTTPS origin, without a path. Set once a domain is chosen.
# SITE_URL=https://your-domain.example

# Vercel: imports stay disabled until this is true. Configure WAF rate limits first.
# Local development: enabled unless explicitly set to false in the process environment.
CHORDI_WEB_IMPORT_ENABLED=false
